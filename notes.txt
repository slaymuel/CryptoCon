TODO:
Robust Reconnect
Probably need a config for websocket also. Would maybe be nice to use the same config
Lots of docstrings

✔ A Beast‑based HTTPS REST client
- GET /fapi/v1/exchangeInfo
- POST /fapi/v1/order
- GET /fapi/v2/balance
- etc.
✔ HMAC SHA256 signing
- Using OpenSSL
✔ URL/query builder
- For Futures endpoints
✔ Error handling
- Binance error codes
- HTTP status codes
Once REST is done, you can move on to:
✔ uWebSockets WebSocket client
- mark price
- book ticker
- depth
- liquidation streams


# Debug build
cmake -B build/debug -DCMAKE_BUILD_TYPE=Debug
cmake --build build/debug

# Release build
cmake -B build/release -DCMAKE_BUILD_TYPE=Release
cmake --build build/release


